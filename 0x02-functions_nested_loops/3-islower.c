#include "main.h"

/**
 * int_islower - A C program that checks for lower case character
*/

int _islower(int c)
{
	int i;

	if (i >= 97 && i <= 122)
		return (1);
	else
		return (0);
	_putchar('\n');
}
