#include "main.h"

/**
 * _puts - prints a string to stdout
 * @str: pointer to the string to print
 */
void print_rev(char *s)
{	
	int i = 0;

	while (str[i])
		i++;

	while (i--)
		_putchar(str[i]);
	_putchar('\n');
}
